/**
 * @ngdoc function
 * @name jtWorkApp.directive:wtSearchResultList
 * @description
 * # wtSearchResultList
 * left menu directive
 */
define(['app'], function (app) {
	'use strict';
	app.directive('wtSearchResultList', 
		['$rootScope','$state','config',
		  function($rootScope,$state,config) {
		  	
	}]);
});